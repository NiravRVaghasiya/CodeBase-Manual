"""A tiny service module used to test src/ layout module resolution."""

from __future__ import annotations


def greet(name: str) -> str:
    """Greet someone by name."""
    return f"Hello, {name}"
